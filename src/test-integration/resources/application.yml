server:
  port: 6060
spring:
  profiles:
    active: test
  redis:
    host: localhost
    port: 6371
  main:
    allow-bean-definition-overriding: false
  jpa:
    hibernate:
      use-new-id-generator-mappings: false

